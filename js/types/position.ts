/**
 * Often used to represent a position in 2D space. 
 * It is named position2D to avoid confusion with the Vector2 class.
 * It is not intended to be used for calculations, but rather to store a x,y position.
 */
export interface Position2D {
    x: number;
    y: number;
}