import { Object3D } from "@wonderlandengine/api";

const teleport = {
    name: "Teleport",

    initialize: function () {
        this.name = `Teleport`;
    },

    picked:function(obj:Object3D, x:number, y:number, z:number){

    }
};